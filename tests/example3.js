describe("Test the example website", () => {
  it("Loads the example website", () => {
    browser.url("http://www.example.com")
  })
})

// no need for npx, though you would on the command line
// since it's not a dependency
